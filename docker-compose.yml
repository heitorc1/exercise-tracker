version: '3.9'

services:
  database:
    image: postgres:15.3
    restart: always
    environment:
      POSTGRES_DB: tracker
      POSTGRES_PASSWORD: example
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
